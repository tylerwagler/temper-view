# Environment variables used by the GPU Dashboard
#
# The base URL for the GPU telemetry API (Temper service)
#
# Example (development):
VITE_GPU_API_BASE=http://10.20.10.5:3001

# For production you would replace this with the actual production endpoint
# VITE_GPU_API_BASE=https://api.yourdomain.com